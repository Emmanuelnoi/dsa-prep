import type { Category } from '@/types'

const category: Category = {
  id: 'sliding-window',
  title: 'sliding-window',
  icon: 'Code',
  group: 'foundations',
  templates: [],
}

export default category
